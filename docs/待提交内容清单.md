# 待提交内容清单

> 截止日期：2026-03-08
> 已提交 4 次 commit，以下为尚未提交到 GitHub 的全部内容。

---

## 一、后端改动（20个文件）

### 1.1 修改的文件（3个）
| 文件 | 改动说明 |
|------|---------|
| `backend/app/__init__.py` | 注册 rec_bp / user_bp / post_bp 三个新 Blueprint |
| `backend/pyproject.toml` | 新增 neo4j / redis / numpy / scipy / faker 等依赖 |
| `backend/uv.lock` | 锁文件同步更新 |

### 1.2 新增 API 路由（3个）
| 文件 | 端点 | 说明 |
|------|------|------|
| `backend/app/api/recommendation.py` | `/api/recommend/*` | 推荐列表 + 推荐理由 + 预计算 |
| `backend/app/api/user.py` | `/api/user/*` | 用户列表 / 详情 / 行为记录 |
| `backend/app/api/post.py` | `/api/post/*` | 帖子列表 / 详情 / 热门 |

### 1.3 新增服务层（7个）
| 文件 | 说明 |
|------|------|
| `backend/app/services/neo4j_service.py` | Neo4j 图数据库驱动封装 |
| `backend/app/services/redis_service.py` | Redis 缓存封装（JSON + Sorted Set） |
| `backend/app/services/recommendation/__init__.py` | 推荐引擎入口，串联三路 Pipeline |
| `backend/app/services/recommendation/cf_engine.py` | Pipeline A: Item-Based CF + IUF |
| `backend/app/services/recommendation/graph_engine.py` | Pipeline B: Neo4j 3跳涟漪传播 |
| `backend/app/services/recommendation/semantic_engine.py` | Pipeline C: Embedding + LLM Reranking |
| `backend/app/services/recommendation/fusion.py` | 三路加权线性融合 |

### 1.4 新增工具函数（1个）
| 文件 | 说明 |
|------|------|
| `backend/app/utils/helpers.py` | cosine_similarity + min_max_normalize |

### 1.5 新增脚本（5个）
| 文件 | 命令 | 说明 |
|------|------|------|
| `backend/scripts/__init__.py` | — | 包标记 |
| `backend/scripts/init_db.py` | `uv run python scripts/init_db.py` | 初始化 MySQL 表 + Neo4j 约束 |
| `backend/scripts/generate_mock_data.py` | `uv run python scripts/generate_mock_data.py` | 生成模拟数据（500用户/2000帖子） |
| `backend/scripts/precompute_recommendations.py` | `uv run python scripts/precompute_recommendations.py` | CF 物品相似度预计算 |
| `backend/scripts/evaluate.py` | `uv run python scripts/evaluate.py` | 7组消融实验评估 |

### 建议提交方式（3次 commit）
```
commit 4: 新增：推荐系统三路融合引擎（CF + Graph + Semantic）
  → services/recommendation/ 全部 + utils/helpers.py + neo4j_service.py + redis_service.py

commit 5: 新增：后端API路由与数据脚本
  → api/post.py + api/user.py + api/recommendation.py
  → app/__init__.py (改动)
  → scripts/ 全部
  → pyproject.toml + uv.lock (改动)

commit 6: 新增：模块笔记文档
  → docs/ 全部
```

---

## 二、前端重构（24个文件）

### 2.1 修改的文件（6个）
| 文件 | 改动说明 |
|------|---------|
| `frontend/src/main.js` | 装 Element Plus + Router + 全局 Icons |
| `frontend/src/App.vue` | 精简为 `<router-view />`（原千问测试页面移除） |
| `frontend/src/style.css` | 去暗色主题，全局样式重置 |
| `frontend/index.html` | title 改为「KnowledgeRec - 知识社区推荐系统」 |
| `frontend/package.json` | 新增 element-plus / icons-vue / vue-router |
| `frontend/package-lock.json` | 锁文件同步 |

### 2.2 删除的文件（1个）
| 文件 | 说明 |
|------|------|
| `frontend/src/components/HelloWorld.vue` | Vite 默认组件，不再使用 |

### 2.3 新增 API 层（4个）
| 文件 | 说明 |
|------|------|
| `frontend/src/api/index.js` | axios 实例（baseURL=/api, 30s超时, 错误拦截） |
| `frontend/src/api/recommendation.js` | getRecommendations / getRecommendReason / triggerPrecompute |
| `frontend/src/api/user.js` | getUserList / getUserDetail / getUserBehaviors |
| `frontend/src/api/post.js` | getPostList / getPostDetail / getHotPosts |

### 2.4 新增路由（1个）
| 文件 | 说明 |
|------|------|
| `frontend/src/router/index.js` | 5 个路由（recommend / posts / posts/:id / users / users/:id） |

### 2.5 新增布局组件（3个）
| 文件 | 说明 |
|------|------|
| `frontend/src/components/layout/AppLayout.vue` | 整体布局：顶栏 + 侧栏 + 内容区 |
| `frontend/src/components/layout/AppNavbar.vue` | 顶部导航栏 |
| `frontend/src/components/layout/AppSidebar.vue` | 左侧菜单（智能推荐/帖子列表/用户列表） |

### 2.6 新增推荐组件（5个）
| 文件 | 说明 |
|------|------|
| `frontend/src/components/recommend/UserSelector.vue` | 用户选择下拉框 |
| `frontend/src/components/recommend/WeightSlider.vue` | 三路权重联动滑块（和=100） |
| `frontend/src/components/recommend/ScoreBar.vue` | 三色分数可视化条 |
| `frontend/src/components/recommend/RecCard.vue` | 推荐结果卡片 |
| `frontend/src/components/recommend/RecReasonDialog.vue` | LLM 推荐理由弹窗 |

### 2.7 新增帖子/用户组件（3个）
| 文件 | 说明 |
|------|------|
| `frontend/src/components/post/PostCard.vue` | 帖子预览卡片 |
| `frontend/src/components/user/UserCard.vue` | 用户预览卡片 |
| `frontend/src/components/user/BehaviorTimeline.vue` | 行为时间线 |

### 2.8 新增页面（5个）
| 文件 | 说明 |
|------|------|
| `frontend/src/views/RecommendPage.vue` | 推荐页（核心，答辩重点） |
| `frontend/src/views/PostListPage.vue` | 帖子列表（领域 tabs + 分页） |
| `frontend/src/views/PostDetailPage.vue` | 帖子详情 |
| `frontend/src/views/UserListPage.vue` | 用户网格列表 |
| `frontend/src/views/UserDetailPage.vue` | 用户详情 + 行为历史 |

### 建议提交方式（2次 commit）
```
commit 7: 重构：前端框架升级（Element Plus + Router + 布局系统）
  → main.js + App.vue + style.css + index.html + package.json + package-lock.json
  → router/ + api/ + components/layout/
  → 删除 HelloWorld.vue

commit 8: 新增：前端推荐页、帖子页、用户页
  → components/recommend/ + components/post/ + components/user/
  → views/ 全部5个页面
```

---

## 三、文档笔记（12个文件）

| 文件 | 说明 |
|------|------|
| `docs/README.md` | 笔记目录索引 |
| `docs/01-项目结构.md` | 前端目录、路由、依赖 |
| `docs/02-API层.md` | axios 封装 + API 接口 |
| `docs/03-推荐页.md` | 推荐核心页面 + 6个组件 |
| `docs/04-帖子页.md` | 帖子列表/详情 |
| `docs/05-用户页.md` | 用户列表/详情 + 时间线 |
| `docs/06-后端总览.md` | 后端架构、目录、配置 |
| `docs/07-数据模型.md` | 6个MySQL模型 + Neo4j图谱 |
| `docs/08-推荐引擎.md` | 三路融合算法详解（论文核心） |
| `docs/09-服务层.md` | 千问/Neo4j/Redis 服务封装 |
| `docs/10-API接口.md` | 全部11个REST端点 |
| `docs/11-数据生成与评估.md` | 数据脚本 + 消融实验 |

---

## 汇总

| 分类 | 新增 | 修改 | 删除 | 合计 |
|------|------|------|------|------|
| 后端 | 16 | 3 | 0 | 19 |
| 前端 | 18 | 6 | 1 | 25 |
| 文档 | 12 | 0 | 0 | 12 |
| **总计** | **46** | **9** | **1** | **56** |
